# Публикуем сам скилл (SKILL.md + references/ в корне). Ниже — что НЕ уходит в репозиторий.

# Прогоны: полный текст чужой книги (.epub) и транскрипты чужих видео → копирайт.
/test-runs/

# Внутренняя кухня проекта, не нужна публике.
/context/
/CLAUDE.md
/.claude/

# OS / редактор / Python
.DS_Store
Thumbs.db
__pycache__/
*.pyc
.idea/
.vscode/
